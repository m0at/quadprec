[package]
name = "quadprec-solver"
version.workspace = true
edition.workspace = true

[features]
default = ["accelerate"]
accelerate = []

[dependencies]
quad = { path = "../quad", package = "hyperprec" }
rayon = { workspace = true }
